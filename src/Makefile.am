AM_CFLAGS = --pedantic -Wall -std=c99 -O2
AM_LDFLAGS = -lcurl -lm

# this lists the binaries to produce, the (non-PHONY, binary) targets in
# the previous manual Makefile
bin_PROGRAMS = bingit
bingit_SOURCES = cJSON.c main.c
